<%= form_for @song  do |f| %>

  <%= artist_select(@song, @artist) %>

  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <% if @song.id.nil? %>
    <%= f.label :artist_name %>
    <%= f.text_field :artist_name %>
  <% end %>
  <br />

  <%= f.submit %>
<% end %>
